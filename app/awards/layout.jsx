export const metadata = {
    title: 'Awards & Recognition',
    description: 'Our achievements and recognition in healthcare excellence',
};

export default function AwardsLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
} 